<section>
    <div class="container"><br>
            <a href="/add"class="btn btn-success ml-right">Add Product</a>
        <div class="row mt-2  ">
              
        </div>
        <table class="table mt-5">
  <thead>
    <tr>
      <th scope="col">No</th>
      <th scope="col">Title</th>
      <th scope="col">Category</th>
      <th scope="col">price</th>
      <th scope="col">Description</th>
      
      <th>Image</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    {{#each data}}
    <tr>
      {{!-- <th scope="row">1</th> --}}
      <td>{{this.id}}</td>
      <td>{{this.name}}</td>
      <td>{{this.category}}</td>
      <td>{{this.price}}</td>
      <td>{{this.discription}}</td>
      <td><img style="width:50px;height:50px;" src="http://localhost:3000/images/{{this.id}}.jpg"alt=""></td>
    <td>
    <a href="edit/{{this.id}}"class="btn btn-sm btn-primary">Edit</a>
    <a href="delete/{{this.id}}"class="btn btn-sm btn-danger" onclick="return confirm('Are you want to delete {{this.name}}?')">Delete</a>
    </td>
    </tr>
    {{/each}}
    
  </tbody>
</table>
    </div>
</section>